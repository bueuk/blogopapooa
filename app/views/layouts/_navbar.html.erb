<!-- Fixed navbar -->
<nav class="navbar transparent navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Papooa', root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="navbar">
      <%= form_tag root_path, method: :get, class: 'navbar-form navbar-left' do %>
              <div class='form-group'>
                      <%= text_field_tag :search, nil, class: 'form-control' %>
              </div>
              <div class='form-group'>
                      <%= submit_tag 'Search', class: 'btn btn-default' %>
              </div>
      <% end %>	

      <% if user_signed_in? %>

        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Halo, <%= current_user.name %>  <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if current_user.admin? %>
                welcome admin
                <li><%= link_to 'Account', users_info_index_path %>
              <% else %>
                <li><%= link_to 'Account', edit_user_registration_path %>
              <% end %>
              <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
            </ul>
        </li>
      </ul>
    <% end %>

        <ul class="nav navbar-nav navbar-left" id="navbar">
          <li><%= link_to 'Categories', categories_path %></li>
          <li><%= link_to 'Archives', archives_path %></li>
          <% if user_signed_in? %>
            <li><%= link_to 'Post', show_all_blog_posts_path %></li>
          <% end %>
          <li><a href="https://github.com/bueuk/" target="_blank"><i class="fab fa-github"> GitHub</i></a></li>
        </ul>
			</div>
	</div>
</nav>
